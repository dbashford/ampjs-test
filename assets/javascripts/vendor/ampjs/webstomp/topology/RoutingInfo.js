define([], function() {
  var RoutingInfo;
  RoutingInfo = (function() {
    function RoutingInfo(routes) {
      this.routes = routes;
    }

    return RoutingInfo;

  })();
  return RoutingInfo;
});
